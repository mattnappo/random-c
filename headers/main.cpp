#include <iostream>
#include <string>
#include "header.h"
using namespace std;

int main() {
  test();
  /*
	Person person;
	person.build();
	while(1) {
		if(person.print() == 0) {
			cout<<"You need to build your person better!"<<endl;
			person.build();
		} else {
			break;
		}
	}
  */
}
